{{/* Default dynamic values */}}
{{- $gitname := "erikenz" -}}
{{- $gitmail := "erikenz2001@gmail.com" -}}

[data]
    gitname = {{ $gitname | quote }}
    gitmail = {{ $gitmail | quote }}

[git]
    autoCommit = true
    autoPush = true
    autoAdd = true # Add changes to the source state after any change

[edit]
    command = "nvim"

[hooks.add]
    post = "bash {{ .chezmoi.sourceDir }}/home/.chezmoiscripts/sanitize-zed-env.sh"

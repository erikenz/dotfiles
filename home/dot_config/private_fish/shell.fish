# --- History control ---
# Fish appends and de-duplicates history automatically.
# Optionally set max history size:
# set -U fish_history_size 32768

# --- Autocompletion ---
# Fish has built-in completions; no need for bash_completion.
# To add extra completions manually:
# source /usr/share/fish/vendor_completions.d/*.fish

# --- PATH setup ---
set -Ux PATH $HOME/.local/bin $PATH
